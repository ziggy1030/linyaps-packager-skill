version: "__MANIFEST_VERSION__"
package:
  id: __PACKAGE_ID__
  name: __PACKAGE_NAME__
  version: __PACKAGE_VERSION__
  kind: app
  description: |
__PACKAGE_DESCRIPTION__

command:
  - __COMMAND__

base: __BASE__
runtime: __RUNTIME__

sources:
__SOURCES__

buildext:
  apt:
    build_depends:
__BUILD_DEPENDS__
    depends:
__RUNTIME_DEPENDS__

build: |
__BUILD_SCRIPT__
